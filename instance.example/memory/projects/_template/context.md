# Project Context

<!--
This is a TEMPLATE. Copy this folder and rename it to match your project name.

Example: If your .env has KOAN_PROJECTS=myapp:/path/to/myapp
Then copy this folder to: memory/projects/myapp/

Kōan auto-injects this file VERBATIM (capped at 80 lines) into the prompt for
the agent loop AND for the /fix, /plan, /implement, /refactor, /review skills.
It's where you put human-curated context the agent should always know about:
architecture, ongoing initiatives, key stakeholders, non-obvious constraints.

This file is human-only. Kōan never edits it automatically. The semantic
compaction pipeline ignores it (no risk of your notes being rewritten).
-->

Architecture notes and important patterns for this project.

## Overview

Brief description of what this project does.

## Architecture

Key architectural decisions, patterns, and conventions.

## Important Paths

Critical files and directories to know about.
